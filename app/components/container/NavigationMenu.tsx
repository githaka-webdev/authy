export default function NavigationMenu({ children }: NavigationMenuProps) {
    return (
        <main>
            <section></section>
            <section>{children}</section>
        </main>
    );
}
